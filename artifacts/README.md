# Artifact policy

Model checkpoints and generated deployment artifacts are ignored by default. Deliberately versioned golden fixtures must be small, deterministic, and documented. Evaluation summaries may be committed. Never commit secrets or API keys.
