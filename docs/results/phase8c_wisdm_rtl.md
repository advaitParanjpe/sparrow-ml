# Phase 8C WISDM RTL validation

Selected test-subject samples execute as four `fc1` Sparrow-V partitions and one `fc2` execution. Bias reconstruction, ReLU, and hidden requantization are host-side. Partitioned totals are not optimized monolithic latency.
